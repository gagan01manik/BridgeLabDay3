package practice;
public class arthmatic {
    public static void main(String[] args) {
        
    
    int x =8, y=3;
    int sum = x+y; // arthmatic
    boolean isEqual = (x==y);  //realtional
    boolean logical = (x>5) && (y<5); // logical
    String result = (x>y)? "x is larger" : "y is larger"; // ternary
}
}
